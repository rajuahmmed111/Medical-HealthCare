export const filterField: string[] = ["searchTerm", "email", "role", "status"];
export const userSearchableFields: string[] = ["email"]; // only email can be searched
export const paginationField: string[] = [
  "limit",
  "page",
  "sortBy",
  "sortOrder",
]; // total searchable fields
